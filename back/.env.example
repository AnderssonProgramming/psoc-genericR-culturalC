# Backend Environment Variables

# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# HMAC Secret (required - must match Unity secret)
SECRET_HMAC_KEY=your-secret-for-hmac-verification-change-this

# AI Configuration (choose one)
# Option 1: Use local Python AI Service with GPT-OSS (RECOMMENDED)
USE_LOCAL_MODEL=true
PYTHON_AI_SERVICE_URL=http://localhost:5000

# Option 2: Use Hugging Face API (fallback)
HUGGINGFACE_API_KEY=your-huggingface-token-here

# JWT Secret (required for authentication)
JWT_SECRET=your-jwt-secret-key-change-this

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
